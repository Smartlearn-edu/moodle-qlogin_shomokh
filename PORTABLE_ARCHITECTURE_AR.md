# مخطط تنفيذ الدخول والتحقق القابل للنقل

هذا الملف هو المرجع التنفيذي لتطبيق تجربة `local_qlogin_shomokh` في Moodle آخر، أو إعادة بناء
الفكرة في Next.js أو Frappe دون تكرار مرحلة الاكتشاف التي أُنجزت في 17 أغسطس 2026. لا يحتوي
أسرارًا أو أرقام تحقق.

## 1. العقود الثابتة

1. للمستخدم معرّف داخلي ثابت لا يتغير. الجوال اسم دخول أو alias فريد، وليس المفتاح الذي تُنقل
   عليه الدرجات أو الالتحاقات.
2. التسجيل ينشئ الحساب فورًا، ثم يبدأ تحقق البريد والجوال خلال مهلة قابلة للضبط؛ الافتراضي 30 يومًا.
3. لكل قناة سجل مستقل: `pending` أو `verified` أو `expired` أو `waived`. مهلة إعادة الإرسال لكل
   قناة مستقلة.
4. المستخدم يبدأ رسالة واتساب بنفسه: `SHOMOKH VERIFY <code>`. لا يلزم OTP مدفوع صادر من
   المنصة.
5. الرمز عشوائي، أحادي الاستخدام، قصير العمر، ويحفظ hash فقط. لا يسجل محتوى الرسالة أو رقم
   الجوال والبريد كاملين في سجل التشخيص.
6. فشل البريد لا يلغي حسابًا نجح إنشاؤه. تعرض نتيجة مفهومة، وتسجل محاولة، ويعاد الفشل المؤقت
   بمعرف idempotency.
7. المدير لا يدخل في مسار تحقق الطالب. له اختبار مستقل `SHOMOKH TEST <code>` لا يغير أي حساب.
8. الحساب القديم الناقص للجوال يثبت ملكيته بجلسة قائمة أو كلمة المرور الحالية، ثم يضيف alias
   فريدًا ويؤكده من واتساب. لا تجوز المطابقة بالاسم، ولا المطابقة الآلية لبريد مكرر.
9. الدخول يقبل الهاتف الحالي أو البريد الفريد أو اسم المستخدم، وكلها تحل إلى
   المعرّف الداخلي نفسه. البريد المكرر لا يُحل تلقائيًا.
10. تغيير الهاتف يتطلب إعادة إثبات كلمة المرور؛ يعطل alias السابق للدخول ويجعل
    الجديد pending دون نقل ملكية البيانات أو تغيير المعرّف الداخلي.
11. الحسابات الجديدة تُوسم بالتتبع عند الإنشاء. اعتماد البريد الموجود تلقائيًا
    محصور بالحساب القديم غير المتتبع حتى لا يتحول وجود البريد إلى إثبات ملكية.
12. حجز الجوال يفحص كل مصادر الهوية القديمة قبل الإنشاء، ويعاد داخل قفل الرقم.
    معالجة الرسالة تطابق `(phone, token_hash)` ولا تعتمد على أول سجل يحمل الرقم.
13. واجهة الهاتف تحفظ بلد picker الفعلي وتتعامل مع لصق الرقم الدولي دون `+`؛
    لا تسمح بجمع مفتاح الدولة المعروض مع المفتاح الموجود داخل الرقم مرة أخرى.

## 2. الوحدات المطلوبة

- **Identity adapter:** إنشاء المستخدم، تسجيل الدخول، جلب المستخدم بالمعرّف، وإدارة phone alias.
- **Verification service:** إنشاء السجل، إصدار الرمز، المطابقة، انتهاء المهلة، والإعفاء.
- **Email adapter:** إرسال مباشر عبر Resend أو مزود واحد محدد، وتسجيل accepted/delivered/bounced.
- **WhatsApp adapter:** رابط `wa.me`، GET challenge، POST webhook، تحقق التوقيع وPhone Number ID.
- **Recovery service:** استعادة مستقلة بالبريد وواتساب، برموز مختلفة عن رموز التحقق.
- **Admin diagnostics:** صحة الإعداد، آخر أحداث مخفية، اختبار بريد، واختبار واتساب كامل.
- **Migration service:** dry-run، كشف التكرار، ثم ربط aliases الفريدة بالمستخدم القديم نفسه.
- **Account claim service:** إثبات حساب قديم، منع تعارض البريد والجوال، وإضافة alias واحد دون
  إنشاء مستخدم أو نقل بياناته.
- **Self-service profile entry:** يعرض لصاحب الحساب حالات القنوات وزر مركز التحقق، ويُحجب عن
  المدير وعند مشاهدة ملفات الآخرين.

لا تجعل route أو صفحة UI تنفذ هذه القواعد مباشرة؛ تستدعي service حتى يمكن اختبارها ونقلها.

## 3. نموذج البيانات الأدنى

### verification

`id, user_id, channel, target, token_hash, state, expires_at, verified_at, last_sent_at,
reminder_count, last_reminded_at, created_at, updated_at`

قيد فريد: `(user_id, channel)`. يخزن `target` بصيغة موحدة؛ أرقام الجوال E.164 دون `+`.

### inbound_event

`id, provider_message_id, user_id nullable, event_type, status, created_at`

قيد فريد إلزامي على `provider_message_id` لمنع إعادة معالجة Meta لنفس الرسالة.

### mail_attempt

`id, user_id nullable, purpose, provider, recipient_hash, recipient_hint, provider_message_id,
idempotency_key, status, http_status, attempts, last_error, created_at, updated_at`

### phone_alias

`id, user_id, phone, source, status, verified, created_by, created_at, updated_at`

قيدان فريدان على `user_id` و`phone` في النموذج الحالي. إذا سمحت المنصة لاحقًا بأكثر من جوال
للمستخدم فغيّر قرار العمل والقيود معًا، لا القيد وحده.

عند الربط الذاتي تكون `source = selfclaim` و`verified = false` حتى تصل رسالة واتساب المطابقة.
تُجرى المطالبة داخل transaction مع قفل للمستخدم والاعتماد النهائي على القيود الفريدة لمنع السباق.

عند تغيير الجوال تكون `source = selfchange`، ويظل اسم المستخدم الداخلي ثابتًا.
يجب أن يتقدم alias النشط في حل الدخول على أي اسم مستخدم رقمي قديم حتى لا يبقى
الرقم المستبدل صالحًا. لا يعيد تغيير اتصال pending مهلة التحقق؛ أما استبدال اتصال
verified فينشئ مهلة جديدة للاتصال الجديد.

## 4. معالجة Webhook من Meta

1. GET: قارن verify token باستخدام مقارنة ثابتة الزمن، ثم أعد challenge فقط عند التطابق.
2. POST: اقرأ الجسم الخام بحد أقصى مناسب قبل تحويل JSON.
3. احسب `sha256=HMAC_SHA256(raw_body, app_secret)` وقارنه بـ`X-Hub-Signature-256`.
4. ارفض التوقيع غير الصحيح قبل أي كتابة.
5. قارن `metadata.phone_number_id` بالمعرف المضبوط للموقع. رقم الأعمال الظاهر وPhone Number ID
   قيمتان مختلفتان.
6. أنشئ قيد deduplication بمعرّف رسالة Meta قبل تنفيذ الأثر الدائم.
7. وجّه النص حسب الغرض: `VERIFY` أو `RESET` أو `TEST`، ثم طابق hash والوجهة عند الحاجة.
8. أعد HTTP 200 للحدث الصحيح حتى إن كانت نتيجة العمل `invalidcode`، كي لا تعيد Meta الحدث.

الاشتراك الحقيقي يجب أن يظهر في `/{WABA_ID}/subscribed_apps`. تحديد `messages` في شاشة Meta
وحده لا يكفي إذا لم يُنشأ اشتراك WABA فعليًا.

## 5. البريد وResend

- المفتاح سر خادمي، ويفضل مفتاح مستقل لكل موقع ونطاق إرسال.
- عنوان `from` يجب أن يكون ضمن نطاق موثّق؛ المفتاح وحده لا يكفي.
- استخدم Idempotency-Key ثابتًا للمحاولة المنطقية نفسها، ولا ترسل الرسالة نفسها عبر مزودين.
- تحقق من توقيع Resend/Svix على الجسم الخام قبل تحديث `mail_attempt`.
- `accepted` يعني قبول API، و`delivered` يأتي من Webhook. اعرض الفرق في صفحة الصحة.

## 6. تطبيق Next.js

بنية مقترحة مع App Router:

- `app/api/webhooks/meta/route.ts`: GET challenge وPOST inbound.
- `app/api/webhooks/resend/route.ts`: تحقق Svix وتحديث التسليم.
- `app/api/verification/email/start/route.ts` و`.../whatsapp/start/route.ts`.
- `app/api/verification/status/route.ts` لإعادة الحالة فقط للمستخدم الحالي.
- `app/admin/integrations/page.tsx` لاختبارات المدير، محميًا بدور صريح.
- أضف بطاقة في صفحة ملف المستخدم الحالي مرتبطة بمركز التحقق؛ لا تعتمد على تنبيه الصفحة وحده.
- `lib/domain/verification.ts` و`lib/providers/{meta,resend}.ts` و`lib/identity.ts`.

اجعل كل الأسرار في متغيرات الخادم أو مدير أسرار، ولا تستخدم متغيرًا يبدأ بـ`NEXT_PUBLIC_` لها.
تحقق من جلسة المدير وCSRF في إجراءات الاختبار، واستخدم transaction/unique constraint لمعالجة
التزامن. لا تثق في JavaScript المتصفح لتقرير نجاح التحقق.

## 7. تطبيق Frappe

- أنشئ DocTypes: `Contact Verification`, `Inbound Provider Event`, `Mail Attempt`, `Phone Alias`.
- ضع الأسرار في `site_config.json` أو مدير أسرار، وليس في DocType قابل للقراءة من المستخدم.
- Webhook method يمكن أن يكون `allow_guest=True` فقط لأنه يتحقق من توقيع المزود أولًا؛ لا يمنحه
  صلاحية لأي عملية أخرى.
- اقرأ `frappe.request.get_data()` الخام قبل JSON، ثم طبق HMAC وdeduplication.
- استخدم `frappe.enqueue` لإعادة المحاولات وscheduler للانتهاء والتذكيرات والتنظيف.
- احم صفحة الصحة وإجراءات الاختبار بدور `System Manager` أو Role مخصص، مع CSRF للإجراءات
  الإدارية العادية.
- أضف زرًا في User form أو صفحة الملف للمستخدم الحالي عبر hook/override في التطبيق، لا بتعديل
  Frappe Core، واعرض حالة كل قناة دون كشف قيم الاتصال.
- اربط الجوال بالمستخدم القديم عبر DocType alias؛ لا تنشئ User جديدًا عند العثور على رقم فريد.

## 8. اختبار المدير المعتمد

اختبار واتساب الإداري يولد challenge لعشر دقائق، يخزن hash في إعدادات الخادم، ويبقي القيمة الخام
في جلسة المدير فقط. يفتح زر واحد `wa.me` للرقم المضبوط. النجاح يجب أن يسجل:

`event_type = integrationtest`, `status = passed`

وهو يثبت معًا: رقم الأعمال، اشتراك WABA، وصول الرسالة، App Secret، Phone Number ID، Webhook،
المطابقة، والكتابة في قاعدة البيانات. لا يشترط تحول علامتي واتساب إلى اللون الأزرق.

## 9. ما ثبت وما يعاد عند النقل

ثبت في بيئة الاختبار: التسجيل بالجوال، منع التكرار، إظهار كلمة المرور، تحقق البريد عبر Resend،
Webhook Meta الموقّع، رسالة واتساب حقيقية، واستعادة/سجلات الإضافة. لا يعيد وكيل جديد هذه
الاختبارات لمجرد استلام المشروع.

في كل موقع جديد اختبر فقط القيم الخاصة به: رابطا Webhook، sender domain، WABA subscription،
رقم الأعمال، Phone Number ID، الأسرار، cron/queue، ثم اختبار المدير للبريد وواتساب. أعد تدفق
الطالب الكامل إذا تغير منطق التسجيل أو مخطط الحالة أو الترحيل.

## 10. ترتيب التنفيذ لأي إطار

1. أنشئ migrations والقيود الفريدة.
2. نفذ services الخالصة واختبارات الحالات والرموز.
3. نفذ webhook verification وdeduplication قبل واجهات المستخدم.
4. أضف provider adapters وإعادة المحاولة.
5. أضف التسجيل والاستعادة والتنبيهات غير المانعة.
6. أضف صفحة صحة واختبار المدير.
7. نفذ dry-run للحسابات القديمة ثم aliases فقط.
8. أضف مسار المطالبة الذاتية وتنبيه الحساب القديم الناقص للجوال، مع مسار دعم يدوي للحالات التي
   لا تستطيع إثبات كلمة المرور أو البريد.
9. اختبر staging، وبعده فعّل مسار الدخول الجديد دون تعديل بيانات المستخدمين الأساسية.
