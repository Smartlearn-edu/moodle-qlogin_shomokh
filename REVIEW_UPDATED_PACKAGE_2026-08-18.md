# تقرير مراجعة الحزمة المحدثة `local_qlogin_shomokh`

التاريخ: 18 أغسطس 2026  
الملف المراجع: `C:\Users\11\Downloads\Compressed\moodle-qlogin_shomokh-main_2.zip`

## الخلاصة

لا أنصح بتثبيت هذه الحزمة أو اعتمادها على الإنتاج بصورتها الحالية. اجتازت الملفات فحوص النحو الأساسية، لكن المراجعة كشفت أخطاء وظيفية لم يكتشفها CodeChecker، كما أن هوية الحزمة لا تطابق البيانات التي أرسلها المطور.

## 1. الحزمة لا تطابق الوصف المرسل

- SHA-256 الفعلي: `93EEFD05DB11AFA9C3303FC801F0684E6991577141ED20F4E34EA55BCF6FAB5D`.
- SHA-256 المرسل من المطور: `ADB94D415611BFF56FB935A98F45972AB4E98C2C7A57D968AEEC781B804511B9`.
- البناء داخل `version.php`: `2026081811`، وليس `2026081810`.
- الإصدار النصي: `0.7.0`.
- مجلد ZIP الجذري: `moodle-qlogin_shomokh-main`، وليس `qlogin_shomokh`.

هذا يعني أن الملف المراجع ليس ملف الإصدار المحدد في رسالة المطور، بل يبدو أرشيف مصدر أحدث من GitHub. ووفق توثيق Moodle يصبح اسم المجلد الجذري في ZIP اسم مجلد الإضافة عند الاستخراج؛ لذلك يلزم إخراج حزمة نشر جذرها `qlogin_shomokh`.

## 2. أخطاء OAuth2 — درجة عالية

في `index.php`، الأسطر 246–270:

- يستدعي الكود `get_all_issuers()` دون `true`. في Moodle 4.1 يستبعد ذلك مزودي `LOGINONLY`، مع أنهم المزودون المخصصون لصفحة الدخول، وقد يعيد مزودي الخدمات غير المسموح لهم بالدخول.
- يفحص `enabled` فقط، ولا يفحص `auth_oauth2` نفسه ولا يستدعي `is_available_for_login()`.
- النتيجة المحتملة: إخفاء مزود صالح أو عرض زر مزود لا يصلح للدخول، ثم ظهور خطأ عند النقر عليه.
- التعرف على Google عبر وجود كلمة `google` في الاسم هش؛ الخاصية `servicetype` أدق.

هناك تعارض معماري أهم:

- Moodle ينشئ الحساب الجديد عبر OAuth بقيمة `auth = oauth2`.
- الإضافة تسمح افتراضيًا فقط بـ `manual,email` في `classes/auth_policy.php`، السطر 34، وفي الإعدادات.
- `classes/verification.php`، الأسطر 224–233، يستبعد أي مستخدم لا تسمح به هذه السياسة.

بالتالي، إذا سمح Moodle بإنشاء حسابات OAuth جديدة، فقد ينشأ حساب بلا جوال ويتجاوز سياسة التحقق والمهلة، ولا يستطيع استخدام بعض وظائف ربط الجوال. يجب تحديد ما إذا كان OAuth للدخول إلى حسابات مرتبطة فقط أم لإنشاء حسابات جديدة، ثم تصميم منع التكرار وربط الجوال قبل تفعيله.

## 3. تنسيق أزرار OAuth غير مكتمل — درجة متوسطة

في `qlogin_styles.css`، الأسطر 359–388:

- كتلة `.btn-google` تحتوي خصائص الفاصل `.divider` بالخطأ، وفيها خصائص مكررة ومتعارضة مثل `margin` و`color`.
- لا توجد قاعدة أساسية لـ `.divider`، ولذلك لا يعمل الفاصل المرئي كما صُمم.
- لا توجد أي قاعدة لـ `.btn-idp` رغم أن `index.php` يستخدمها للمزودين غير Google.

إذن ادعاء اكتمال أزرار OAuth وعزل تنسيقاتها غير صحيح في هذه النسخة.

## 4. عزل CSS ما زال غير كامل — درجة متوسطة

في `qlogin_styles.css`، الأسطر 577–596، بقيت هذه المحددات عامة خارج `#qlogin-wrapper`:

- `body.dir-rtl .iti__flag-box`
- `body.dir-rtl .iti__country-name`
- `body.dir-rtl .iti__dial-code`

قد تؤثر في أي حقل هاتف آخر داخل الموقع. يجب إضافة `#qlogin-wrapper` إلى المحددات الثلاثة، ثم اختبار القائمة على Educard بالعربية والإنجليزية وعلى الهاتف وسطح المكتب.

## 5. استعلام Cron غير فريد — درجة متوسطة

في `classes/task/process_verifications.php`، الأسطر 60–70:

- يختار المستخدمين المنتهية مهلتهم بـ `DISTINCT userid`.
- ثم يستخدم `$DB->get_record()` بشرط `userid + state` فقط.
- جدول التحقق يسمح بسجلين للمستخدم نفسه، بريد وجوال؛ والفهرس الفريد هو `userid,channel` في `db/install.xml`، السطر 25.

عندما ينتهي البريد والجوال معًا قد يطابق الاستعلام سجلين. هذا استعمال غير صحيح لـ `get_record()` وقد يولد تحذير Debug ويختار سجلًا غير محدد، فتكون عدادات التذكير مرتبطة بقناة عشوائية. يلزم اختيار سجل واحد بصورة صريحة وحتمية إذا كان التذكير لكل مستخدم، أو معالجة كل قناة على حدة إذا كان التذكير خاصًا بالقناة، مع اختبار الحالتين.

## 6. عبارة «جاهزة للإنتاج 100%» غير مثبتة

- نجح `php -l` محليًا لجميع ملفات PHP وعددها 69.
- نجح `node --check` لملفي JavaScript.
- `db/install.xml` صالح نحويًا.
- مفاتيح اللغة العربية والإنجليزية متطابقة في العدد.
- لم أجد أسرار Resend أو Meta حقيقية، ولا مجلد `.git`، ولا مسارات ZIP خطرة.
- توجد 37 دالة اختبار PHPUnit في المصدر، لكن لا يوجد داخل الحزمة ناتج تشغيل PHPUnit.
- لا توجد اختبارات لـ OAuth أو للمهمة `process_verifications`.
- خطة الجاهزية المرفقة تحتوي 67 اختبارًا غير منفذ و0 اختبارات مؤشرة كمجتازة، كما أنها ما زالت معنونة للإصدار 0.6.2.

نجاح `validate/phplint/phpcs/mustache` مهم، لكنه لا يثبت صحة التدفقات الوظيفية أو سلامة التكامل مع OAuth وCron والقالب.

## أمور تأكدت سلامتها

- إصلاح تحميل `moodleform` موجود في النماذج العشرة: `MOODLE_INTERNAL` و`formslib.php` موجودان.
- خطأ الاستدعاء القديم `remind()` بلا معامل لم يعد موجودًا؛ الاستدعاء الحالي يمرر سجلًا.
- جزء عزل شبكة Bootstrap داخل `#qlogin-wrapper` موجود.
- المصدر يطابق تقريبًا الفرع العام الحالي للمستودع، لكنه ليس حزمة الإصدار ذات البصمة التي ذكرها المطور.

## الأسئلة المطلوب إرسالها للمطور

1. هل يمكن إرسال ملف `qlogin_shomokh_v0.7.0.zip` نفسه الذي بصمته `ADB94...`، لا أرشيف GitHub، مع جعل المجلد الجذري `qlogin_shomokh` وتأكيد رقم البناء النهائي؟
2. هل OAuth مطلوب لإنشاء حسابات جديدة أم للدخول إلى حسابات Moodle مرتبطة سابقًا فقط؟ وكيف سيُمنع إنشاء حساب مكرر بالبريد نفسه ويُفرض ربط الجوال والتحقق منه؟
3. لماذا استُخدم `get_all_issuers()` بدل `get_all_issuers(true)` مع `auth_oauth2::is_enabled()` و`is_available_for_login()`؟
4. هل شُغّلت PHPUnit فعلًا؟ نحتاج ناتج الأمر، لا نتائج CodeChecker فقط، وإضافة اختبارات OAuth وCron عند انتهاء القناتين معًا.
5. هل اختُبرت الواجهة فعليًا على قالب Educard في RTL/LTR والهاتف؟ نحتاج صورًا أو نتيجة Behat/اختبار يدوي مكتملة.

## خطة التصحيح المقترحة

1. إيقاف OAuth افتراضيًا أو إزالته مؤقتًا حتى يُحسم تدفق الحسابات الجديدة والربط ومنع التكرار.
2. إن تقرر إبقاؤه: فحص تفعيل `auth_oauth2`، جلب مزودي الدخول جميعًا، استخدام `is_available_for_login()`، وإدخال `oauth2` في سياسة تحقق مصممة ومختبرة، لا إضافته نصيًا فقط.
3. إصلاح `.btn-google` وإضافة `.btn-idp` و`.divider` وحصر كل قواعد RTL داخل `#qlogin-wrapper`.
4. إصلاح اختيار سجل Cron وإضافة اختبار لمستخدم لديه بريد وجوال منتهيان، واختبار سجل قناة واحدة.
5. تشغيل PHPUnit ثم الاختبارات اليدوية الـ67 على staging مطابق للإنتاج، خصوصًا التسجيل، OAuth، التحقق، الاستعادة، الترحيل، Cron، وواجهة Educard.
6. إنشاء ZIP نشر نظيف جذرُه `qlogin_shomokh`، وتثبيت رقم بناء واحد، وحساب SHA-256 بعد إنشاء الملف النهائي مباشرة.

## المراجع الرسمية

- Moodle 4.1 OAuth API: https://github.com/moodle/moodle/blob/MOODLE_401_STABLE/lib/classes/oauth2/api.php
- Moodle 4.1 OAuth issuer: https://github.com/moodle/moodle/blob/MOODLE_401_STABLE/lib/classes/oauth2/issuer.php
- Moodle 4.1 OAuth login endpoint: https://github.com/moodle/moodle/blob/MOODLE_401_STABLE/auth/oauth2/login.php
- Moodle 4.1 OAuth account creation: https://github.com/moodle/moodle/blob/MOODLE_401_STABLE/auth/oauth2/classes/api.php
- Moodle plugin ZIP handling: https://phpdoc.moodledev.io/4.1/d3/df2/classcore__plugin__manager.html
