# CONTEXT.md

## الهدف

`local_qlogin_shomokh` إضافة Moodle موحدة لتقليل فقد الطالبات والحسابات المكررة. الجوال هو وسيلة الدخول الأساسية، والبريد وواتساب قناتا تحقق واستعادة. المستخدم يستطيع متابعة المنصة أثناء مهلة قابلة للضبط، والافتراضي 30 يومًا.

## الأصل والحالة الحالية

- بدأت من المستودع العام `Smartlearn-edu/moodle-local_qlogin`، لكنها مكوّن مستقل باسم `local_qlogin_shomokh`.
- لا تعدّل `local_qlogin` المركبة ولا Moodle Core.
- بيئة الاختبار التي استُخدمت: `https://test.smartlearn.education`.
- شوهد على بيئة الاختبار Moodle 5.1.5 وPHP 8.4.23 وMariaDB 11.3.2، لكن الإنتاج غير مؤكد ويجب فحصه قبل النشر.
- الإصدار 0.4.2 ثُبّت بنجاح على بيئة الاختبار، وأضيف زر إظهار/إخفاء كلمة المرور واختُبر في المتصفح.
- إعداد SMTP في بيئة الاختبار كان فارغًا، وآخر تشغيل ظاهر لمهام Moodle كان 30 يونيو 2026؛ لذلك لم تصل رسالة التحقق التي كانت تعتمد على `email_to_user` وadhoc task.

## قرارات الإصدار 0.4.0

1. رسائل الإضافة المهمة تُرسل مباشرة عبر Resend API عند اختياره، دون الاعتماد على SMTP في Moodle للإرسال الأول.
2. يبقى مزود Moodle خيارًا بديلًا للتوافق، لكن لا يحدث إرسال مزدوج.
3. لا تُستخدم Resend PHP SDK حتى لا تضيف Composer vendor متعارضًا؛ يستخدم الكود Moodle `\curl`.
4. مفتاح Resend الخارجي في `$CFG->local_qlogin_shomokh_resendapikey` أو متغير البيئة `LOCAL_QLOGIN_SHOMOKH_RESEND_API_KEY` يتقدم على القيمة المدخلة من الإدارة.
5. فشل مؤقت في Resend يضع إعادة محاولة بلا رمز خام في بيانات المهمة. الفشل الدائم يظهر في health/log ولا يحذف الحساب.
6. استعادة البريد تصبح مسارًا مستقلًا داخل الإضافة برمز hash أحادي الاستخدام، بدل `core_login_process_password_reset` الذي يعتمد على بريد Moodle.
7. Webhook منفصل من Resend يحدث حالات delivered/bounced/complained/failed بعد تحقق توقيع Svix والجسم الخام.
8. الحسابات القديمة لا يعاد إنشاؤها ولا تتغير أسماؤها تلقائيًا. جدول alias يربط الجوال بـ`userid` نفسه، وتوجد أداة dry-run ثم ربط آمن للأرقام الفريدة فقط.
9. الأرقام المكررة أو الغامضة تُترك للمراجعة اليدوية؛ قد يكون اشتراك الجوال بين أفراد الأسرة حالة حقيقية.
10. Moodle cron يبقى مطلوبًا لصحة Moodle وإعادة المحاولات والمعالجة الدورية، حتى مع الإرسال المباشر عبر Resend.

## قرارات الإصدار 0.4.1

1. سبب الأزرار الثلاثة كان تشغيل واجهتي `local_phoneverify` القديمة والإضافة
   الموحدة معًا؛ كما أن الصفحة القديمة تقرأ إعداد رقم واتساب مستقلًا.
2. مركز التحقق الموحد هو المسار الوحيد للطالب: الرابط القديم يتحول إليه، والتنبيه
   السفلي القديم يُخفى، والتنبيه الموحد يحتوي زرًا واحدًا واضحًا.
3. زر واتساب الرئيسي يفتح رسالة `SHOMOKH VERIFY` الجاهزة، وعند العودة تتحدث
   الصفحة تلقائيًا. الرمز اليدوي وتغيير الرمز داخل أقسام مساعدة مطوية.
4. تعطيل الإضافة القديمة ليس تلقائيًا أثناء الترقية. صفحة الصحة تستورد فقط حالات
   verified/waived ذات الجوال المطابق، ثم تعطل إعدادها، وتبقي جداولها للرجوع.

## قرارات الإصدار 0.4.2

1. أظهر الاختبار أن تنبيه footer يقع بعد تقويم لوحة التحكم على مسافة تقارب
   شاشتين؛ لذلك نُقل إلى hook أعلى جسم الصفحة في Moodle 4.4+.
2. نجح اختبار Meta للمسار والـApp Secret، بينما بقيت رسالة حقيقية معلقة. السبب
   المرجح هو اختلاف Phone Number ID؛ لذلك أضيف سجل `wrongphoneid` وجدول حالات
   واتساب في صفحة الصحة.
3. لم يظهر طلب جديد في Resend Logs بعد محاولة الطالب، واتضح أن المفتاح وحده لا
   يكفي: `resendfromemail` إلزامي ويجب أن ينتمي إلى نطاق موثّق.
4. تم تصحيح Resend Webhook في بيئة الاختبار من جذر نطاق الإنتاج إلى:
   `https://test.smartlearn.education/local/qlogin_shomokh/resend_webhook.php`.

## نتائج الاختبار الفعلية — 17 أغسطس 2026

- أكمل Meta طلب التحقق التجريبي إلى Webhook. ظهور `configuration/wrongphoneid` لعينة Meta كان
  متوقعًا لأن العينة تستخدم Phone Number ID وهميًا، وأثبت أن الرابط والتوقيع وApp Secret تعمل.
- لم يكن اشتراك التطبيق في WABA موجودًا فعليًا رغم ظهور الخيار في الواجهة. نُفذ الاشتراك عبر
  Graph API ثم أكد طلب القراءة أن التطبيق نفسه أصبح ضمن `subscribed_apps` للـWABA.
- بعد الاشتراك وصلت رسالة واتساب حقيقية وسجلت الإضافة `verification/verified`. العلامتان
  الرماديتان كانتا كافيتين؛ لا تعتمد المعالجة على إيصال القراءة الأزرق.
- أرسل Resend رسالة تحقق فعلية، وقُبلت من المزود، واكتمل تحقق البريد. الشرطان الحاسمان هما مفتاح
  API وعنوان مرسل من نطاق موثّق.
- أزيل `local_phoneverify` القديم بعد معالجة خطأ جدوله، وأصبحت الإضافة الموحدة هي المسار الوحيد.
- لا توجد مفاتيح أو رموز تحقق أو access tokens في المستودع أو هذه الوثائق.

هذه النتائج لا تحتاج إلى إعادة لمجرد استلام وكيل جديد للمشروع. يعاد فقط الاختبار المتأثر عند تغيير
الكود أو الموقع أو إعدادات Meta/Resend. أما كل موقع جديد فيحتاج اختبارًا واحدًا لإعداداته الخاصة.

## قرارات الإصدار 0.4.3

1. حساب مدير الموقع وكل من يحمل `local/qlogin_shomokh:manage` لا يرى تنبيه الطالب ولا صفحة
   «مركز التحقق»؛ فتح رابطها يحوله إلى صفحة صحة التكامل.
2. أضيف اختبار واتساب كامل من حساب المدير: رمز مؤقت لا يحفظ خامًا، وزر يفتح واتساب، ومعالجة تمر
   عبر Webhook الحقيقي وتظهر `integrationtest/passed` دون إنشاء طالبة أو تعديل حالة مستخدم.
3. الاختبار صالح عشر دقائق، مربوط بالمدير والجلسة التي أنشأته، وأثره الوحيد سجل تشخيص مختصر.
4. هذا هو المسار المعتمد بعد تغيير الموقع أو رقم الأعمال أو Phone Number ID، بدل الخروج من حساب
   الإدارة وتسجيل حساب طالبة جديد في كل مرة.

## قرارات الإصدار 0.4.4

1. أضيف callback رسمي من Moodle باسم `local_qlogin_shomokh_myprofile_navigation` بدل تعديل
   Core أو القالب.
2. يرى صاحب الحساب المؤهل في `/user/profile.php` حالة البريد والجوال وزرًا واضحًا للوصول إلى
   `verify.php`، سواء كانت الحالة مكتملة أو معلقة.
3. لا يظهر القسم لمدير الموقع، أو لمن يحمل صلاحية إدارة الإضافة، أو لمن يشاهد ملف مستخدم آخر؛
   وبذلك يبقى فصل واجهة الطالب عن صحة التكامل الإداري.

## قرارات الإصدار 0.5.0

1. الحساب القديم الناقص للجوال لا يُطابق بالاسم ولا يُعاد إنشاؤه. يستطيع صاحبه إثبات الحساب
   بالبريد أو اسم المستخدم مع كلمة المرور، ثم إضافة جوال فريد والتحقق منه عبر واتساب.
2. إذا كان صاحب الحساب مسجلًا دخوله، تكفي جلسته الحالية ويظهر له تنبيه وزر في ملفه الشخصي؛
   وإذا نسي كلمة المرور يستخدم استعادة البريد ثم يعود إلى مسار الربط.
3. الجوال يضاف إلى جدول alias وإلى `phone1` فقط إذا كان فارغًا، مع بقاء `userid` واسم المستخدم
   والدرجات والالتحاقات دون تغيير. نجاح واتساب يحدّث حالة alias إلى verified.
4. البريد الموجود يمنع إنشاء حساب مكرر ويوجّه إلى «ربط حساب سابق». البريد المكرر بين حسابين لا
   يُحل تلقائيًا، والجوال المرتبط بحساب آخر لا يمكن المطالبة به.
5. إعداد `selfclaimenabled` يتيح للإدارة تشغيل الربط الذاتي أو إيقافه دون تعديل الكود.

## قرارات الإصدار 0.6.0

1. حقل الدخول واحد ويقبل الجوال أو البريد أو اسم المستخدم. البريد لا يحل إلى
   حساب إلا إذا كان فريدًا؛ البريد المكرر يتطلب اسم المستخدم أو تدخل الدعم.
2. لا يُشترط اكتمال التحقق للسماح بالدخول. الحساب الجديد يستطيع الدخول ببريده
   منذ إنشائه، وتستمر سياسة المهلة والتذكير بصورة مستقلة.
3. يستطيع المستخدم المسجل تغيير الجوال بعد إعادة إدخال كلمة المرور الحالية.
   alias الجديد يحل إلى `userid` نفسه، والجوال السابق يتوقف عن الدخول فورًا،
   ثم تصبح قناة الجوال pending حتى يكتمل واتساب.
4. إذا كان الجوال السابق غير موثّق، لا يعيد تغييره مهلة التحقق. إذا كان موثّقًا
   ثم استُبدل، تبدأ مهلة جديدة للجوال الجديد لأنه حدث اتصال جديد بعد تحقق مكتمل.
5. تبدأ مهلة الحساب القديم من أول دخول ناجح حتى إذا كان الجوال مفقودًا، بسجل
   phone pending فارغ الهدف. لا يمكن تأجيل البداية بعدم إضافة الجوال.
6. الحساب القديم الذي لا يملك أي سجل تحقق يُعتمد بريده الصحيح الموجود ثم يبدأ
   طلب الجوال. كل حساب جديد يسجل قنواته عند `user_created` ولا يعتمد بريده
   تلقائيًا. توجد للإدارة دفعة صريحة لاعتماد بريد الحسابات القديمة قبل دخولها.
7. لا تتغير أسماء المستخدمين الداخلية جماعيًا. alias الحالي يتقدم على اسم
   المستخدم الرقمي القديم، ولذلك يرفض الجوال القديم بعد استبداله.

## إصلاح الإصدار 0.6.1

1. كشف الاختبار الفعلي وصول رسالتين إلى Webhook بحالة `alreadyverified` مع بقاء
   الحساب الحالي pending. السبب كان وجود سجل موثق أقدم للرقم نفسه واختيار أول
   سجل بالرقم دون فحص الرمز.
2. أصبح Webhook يجمع سجلات الرقم ويطابق hash الرمز مع السجلات غير المكتملة أولًا؛
   لذلك يستطيع إصلاح الحالة القديمة القائمة دون حذف سجل أو نقل حساب.
3. فحص التسجيل وتغيير الجوال موحد في `migration::phone_in_use()`، ويشمل username
   وalias وسجل التحقق و`phone1` و`phone2`. هذا يمنع إنشاء حساب جديد برقم موجود
   فقط في بيانات Moodle القديمة.
4. يحيط التسجيل وتغيير الجوال قفل hash للرقم، مع بقاء قيود قاعدة البيانات خط
   الدفاع النهائي عند الطلبات المتزامنة.

## إصلاح الإصدار 0.6.2

1. الحساب `userid=14` في بيئة الاختبار كان يحمل `phone1=966966534390821`، بينما
   رقم واتساب الحقيقي `966534390821`. لذلك وصل Webhook بحالة `unmatchedphone`؛
   المشكلة لم تكن في Meta أو رسالة المستخدم.
2. السبب هو لصق رقم يبدأ بمفتاح الدولة دون `+` داخل حقل يعرض المفتاح منفصلًا،
   ففسرته بعض المتصفحات كرقم محلي وأعادت إضافة المفتاح.
3. أصبح JavaScript يختبر الرقم الملصق كتفسير دولي ويعتمد التفسير الصالح، ويرسل
   مفتاح الدولة المختار مع النموذج دون افتراض دولة ثابتة.
4. أضيف إصلاح إداري حتمي من صفحة الترحيل حسب User ID؛ يصحح alias و`phone1`
   وهدف التحقق، ويحافظ على المهلة والهوية وسجل التحقق التاريخي.

## إصلاح الإصدار 0.6.3

1. وُحّدت طرق المصادقة المسموحة في `auth_policy`، ويشمل الافتراضي حسابات
   `manual` و`email`. كل مسارات الربط والتحقق والاستعادة والترحيل تحترم القائمة
   الإدارية نفسها دون استثناءات ثابتة داخل كل خدمة.
2. ترقية الموقع توسع القيمة التاريخية `manual` إلى `manual,email` فقط؛ أما أي
   قائمة مخصصة أخرى فتحافظ عليها. بذلك تدخل الحسابات القديمة المنشأة عبر
   email self-registration في مهلة تحقق الجوال دون إنشاء حساب جديد.
3. استُبدل ثابت `PARAM_DIGITS` غير الموجود في Moodle بـ`PARAM_INT` لمفتاح
   الدولة في نماذج التسجيل والربط والاستعادة.
4. قوائم الأسماء في الإدارة والترحيل تستخدم حقول الاسم التي يطلبها Moodle،
   وقائمة الدول تُلحق بجسم الصفحة وتُحصر داخل الشاشة لتفادي قصها أو تداخلها في RTL.

## إصلاح الإصدار 0.6.4

1. تسبب نقل قائمة الدول مؤقتًا إلى `document.body` في اختلاف مرجع التموضع مع RTL،
   فظهرت القائمة منفصلة إلى يسار بطاقة التسجيل.
2. أُعيد `dropdownContainer: root || document.body` وكامل تنسيق القائمة إلى
   النسخة المجربة قبل هذا التغيير، مع الإبقاء على إصلاحات الحسابات والمصادقة.

## إصلاح الإصدار 0.6.5

1. أظهر اختبار 0.6.4 أن حاوية `root` ليست هي السلوك المجرب؛ لأنها ذات إزاحة
   مستقلة فتضاعفت إحداثيات القائمة وظهرت في أقصى يمين الصفحة.
2. أزيل `dropdownContainer` ليستخدم `intl-tel-input` الحاوية الأصلية داخل حقل
   الهاتف، وأعيدت قواعد العرض المثبتة في النسخة السابقة السليمة.

## خريطة النقل إلى منصات أخرى

المصدر التنفيذي المفصل هو `PORTABLE_ARCHITECTURE_AR.md`. الخلاصة أن منطق الهوية والتحقق
والاستعادة مستقل عن Moodle، بينما Moodle Forms و`$DB` والصلاحيات وcron مجرد adapters. عند النقل
إلى Next.js أو Frappe تُحافظ حالات السجل، وتوقيع Webhook، ومنع التكرار، والرموز المشفرة، وترحيل
الحسابات بالـalias، وتستبدل فقط طبقة الإطار والتخزين والمهام.

## أسرار وإعدادات Resend

- لا تنسخ مفتاح مشروع `shomokh3` تلقائيًا ولا ترفعه داخل الحزمة.
- أنشئ مفتاحًا مستقلًا بصلاحية Sending access ومقيدًا بنطاق الإرسال.
- النطاق المقترح: `mail.shomokalelm.com` بعد SPF وDKIM، ثم DMARC تدريجيًا.
- الأفضل مفتاح ثانٍ منفصل إذا ضُبط Resend SMTP لبريد Moodle العام.
- إعدادات غير سرية من لوحة Moodle: المزود، اسم/عنوان المرسل، timeout، عدد المحاولات، مدة الاستعادة والاحتفاظ بالسجلات.

## مسار الانتقال للإنتاج

1. نسخة احتياطية من DB و`moodledata` وتجهيز staging مطابق.
2. تثبيت الإضافة مع إبقاء مسار الدخول الحالي دون تغيير.
3. ضبط Resend وإرسال رسالة اختبار، ثم فحص Webhook وcron.
4. تشغيل تقرير الحسابات القديمة وتصدير/مراجعة التعارضات.
5. ربط الأرقام الفريدة فقط على دفعات، وإتاحة الربط الذاتي للحسابات الناقصة؛ لا تغيير لـ`userid`.
6. اختبار دخول الحسابات الجديدة والقديمة بالجوال، واستعادة البريد وواتساب.
7. تعطيل الإضافات القديمة المتداخلة بعد التحقق، ثم تفعيل Alternate login URL ومراقبة النتائج.

## خارج النطاق الحالي

- القبول الأكاديمي والإلحاق بالدبلومات والمرفقات والمراجعين نظام مستقل لاحقًا.
- إرسال OTP مدفوع من واتساب غير مطلوب؛ المستخدم يبدأ رسالة واتساب بنفسه.
- تحويل Moodle إلى تطبيق أو نظام أوسمة ليس جزءًا من إضافة الدخول والتحقق.
