# تسليم وتركيب local_qlogin_shomokh 0.6.5

## قبل التركيب

1. نفّذ على staging مطابق للإنتاج، وخذ نسخة احتياطية من قاعدة البيانات و`moodledata`.
2. تأكد من إصدار Moodle وPHP وقاعدة البيانات، ومن عمل Moodle cron. SMTP مطلوب
   لبقية رسائل Moodle حتى إن استخدمت الإضافة Resend مباشرة.
3. لا تستبدل `local/qlogin` ولا تعدّل `Alternate login URL` في هذه المرحلة.
4. لا ترفع `config.php` أو App Secret أو Webhook token إلى GitHub.

## الترقية

1. ارفع ZIP من: **Site administration → Plugins → Install plugins**.
2. يجب أن يظهر المكوّن `local_qlogin_shomokh` والإصدار `0.6.5 (2026081801)`.
3. أكمل `/admin/index.php` ثم احفظ الإعدادات. القيمة الافتراضية للمهلة 30 يومًا،
   والقناتان مطلوبتان، وما بعد المهلة تذكير فقط.
4. شغّل cron مرة واحدة لمعالجة المهام المتأخرة، ثم تأكد أنه يعمل كل دقيقة.
5. بعد اكتمال إعداد واتساب افتح **صحة تكامل شموخ** واضغط **استيراد الحالات
   المكتملة وتعطيل الإضافة القديمة**. يفحص الإجراء تطابق الجوال قبل النقل،
   ويعطل مسارها دون حذف بياناتها. لا تلغ تثبيتها قبل النسخة الاحتياطية والمراجعة.

## ربط Resend

1. في Resend أضف نطاقًا فرعيًا مخصصًا، مثل `mail.shomokalelm.com`، وأكمل SPF
   وDKIM. أضف DMARC تدريجيًا بعد التأكد من التسليم.
2. أنشئ مفتاحًا جديدًا بصلاحية **Sending access** ومقيدًا بالنطاق. لا تعِد استخدام
   مفتاح تطبيق آخر في الإنتاج.
3. الأفضل وضع المفتاح خارج قاعدة البيانات في `config.php`:

   `$CFG->local_qlogin_shomokh_resendapikey = 'ضع المفتاح في الخادم فقط';`

   أو في متغير البيئة `LOCAL_QLOGIN_SHOMOKH_RESEND_API_KEY`. لا تضع القيمة في ZIP
   أو GitHub.
4. من إعدادات الإضافة اختر **Resend API**، ثم اكتب عنوان المرسل الموثق واسمه.
   لا يكفي مفتاح API وحده. يجب أن يكون العنوان من نطاق حالته `verified` في Resend.
5. افتح **صحة تكامل شموخ** وأرسل رسالة إلى بريد اختبار مصرح به. يجب أن تظهر
   الحالة `accepted` أو `delivered`.
6. في Resend أنشئ Webhook إلى:

   `https://YOUR-DOMAIN/local/qlogin_shomokh/resend_webhook.php`

   اختر أحداث `sent`, `delivered`, `delivery_delayed`, `bounced`, `complained`,
   `failed`, `suppressed`. ضع Signing secret في إعداد الإضافة أو خارجيًا في
   `$CFG->local_qlogin_shomokh_resendwebhooksecret`.
7. أنشئ مفتاح Resend منفصلًا إذا استخدمت Resend SMTP لبقية بريد Moodle.

## نقل الحسابات القديمة

1. افتح **ترحيل الحسابات الحالية** وشغّل التقرير ونزّل CSV المخفي قبل تغيير مسار الدخول.
2. راجع الأرقام المكررة والغامضة يدويًا؛ لا تفترض أن مشاركة الجوال خطأ دائمًا.
3. اضبط مفتاح الدولة الافتراضي للأرقام المحلية القديمة فقط إذا كانت قاعدة
   البيانات القديمة تستخدم أرقامًا تبدأ بصفر.
4. اضغط **ربط الأرقام الآمنة** على staging. يضيف الإجراء alias ولا يغير
   `user.username` أو `userid` أو الدرجات أو الالتحاقات.
5. اختبر عينة من الحسابات القديمة بالجوال وكلمة المرور القديمة، ثم كرر الدفعة
   إن بقيت أرقام آمنة.
6. اترك **الربط الذاتي للحسابات السابقة** مفعّلًا للحسابات الناقصة للجوال. يثبت
   صاحب الحساب ملكيته بكلمة المرور، أو بجلسة قائمة، ثم يربط جواله ويتحقق منه.
   البريد المكرر أو الحساب الذي لا يمكن إثباته يُحال للدعم ولا يُطابق بالاسم.
7. إذا كانت المنصة القديمة تعد البريد الموجود مؤكدًا، نفّذ من صفحة الترحيل
   **اعتماد البريد الموثق للحسابات القديمة** على دفعات. لا تنفذ هذا الإجراء قبل
   تأكيد سياسة البيانات، ولا تستخدمه لاعتماد بريد الحسابات الجديدة.

## ربط Meta WhatsApp Cloud API

من إعدادات الإضافة أدخل:

- **WhatsApp business number:** رقم العرض الحقيقي الذي سيفتحه زر `wa.me`، بصيغة
  دولية من 8 إلى 15 رقمًا. لا تضع WhatsApp Business Account ID أو Phone Number ID هنا.
- **Meta phone number ID:** معرّف الرقم من Meta.
- **Webhook verify token:** قيمة سرية طويلة تنشئها أنت وتضع القيمة نفسها في Meta.
- **Meta app secret:** App Secret من لوحة Meta، ولا يرسل لأي شخص في رسالة عادية.

في Meta اضبط Callback URL على:

`https://YOUR-DOMAIN/local/qlogin_shomokh/webhook.php`

ثم اشترك في حدث `messages`. يستقبل المسار GET للتحدي وPOST للرسائل. لا يحتاج
Access Token لأن المستخدم هو الذي يبدأ الرسالة؛ لكنه يرفض أي POST لا يحمل
توقيع Meta الصحيح أو لا يطابق Phone Number ID المضبوط.

## اختبار القبول

استخدم حسابات وأرقام اختبار فقط:

ابدأ من حساب المدير بصفحة **صحة تكامل شموخ**: أرسل بريد الاختبار، ثم اضغط
**إنشاء اختبار تكامل واتساب** وأرسل الرسالة الجاهزة. نجاح `integrationtest/passed`
يفحص الربط الكامل دون الخروج أو إنشاء حساب طالبة. كرر هذا الاختبار عند تغيير
الموقع أو رقم الأعمال أو Phone Number ID أو WABA/App أو الأسرار.

1. تسجيل من Android Chrome وiPhone Safari وWindows Chrome مع دول مختلفة، مع
   التأكد من وضوح الألوان وتسلسل التنقل بلوحة المفاتيح.
2. رفض رقم محلي غامض، وقبول الرقم بعد اختيار الدولة، ومنع تكرار الجوال والبريد.
3. دخول صحيح وخاطئ بالجوال والبريد الفريد واسم المستخدم، ثم بقاء الوصول متاحًا
   قبل التحقق. أنشئ بريدًا مكررًا اختباريًا وتأكد أنه لا يختار حسابًا تلقائيًا.
4. وصول رابط البريد مباشرة عبر Resend، نجاحه مرة واحدة، وتصحيح البريد وإبطال
   الرابط القديم، ثم اختبار الاستعادة البريدية المستقلة.
5. فتح واتساب من مركز التحقق، وإرسال الرسالة من الرقم نفسه، ثم ظهور الحالة مكتملة.
   يجب أن يظهر زر واتساب رئيسي واحد، وأن تتحدث الحالة تلقائيًا عند العودة.
6. رفض رسالة من رقم آخر أو رمز خاطئ أو Webhook بلا توقيع، وعدم معالجة `messageid` مرتين.
7. استعادة كلمة المرور بالبريد من نموذج الإضافة المستقل، ثم برسالة واتساب `RESET`،
   وتعيين كلمة جديدة دون إرسالها نصًا.
8. تغيير المهلة والإجراء من الإعدادات دون تعديل الكود.
9. اختبار إعفاء مستخدم وcohort، والتأكد أن الإضافة لا تفك إيقافًا إداريًا سابقًا.
10. مراجعة PHP/Moodle logs وتشغيل `php -l` وcron بعد الترقية.
11. اختبار صفحة صحة التكامل، وتوقيع Resend Webhook، وتقرير الحسابات القديمة دون
    تعديل الأسماء أو إنشاء حسابات مكررة.
12. فتح رابط `/local/phoneverify/verify.php` القديم والتأكد من تحويله إلى المركز
    الموحد، وعدم ظهور تنبيه `local_phoneverify` السفلي.
13. مقارنة **Meta Phone Number ID المضبوط** في صفحة الصحة بالرقم الظاهر بجانب
    رقم الأعمال في Meta؛ اختلافهما يظهر في السجل بالحالة `wrongphoneid`.
14. التأكد أن مدير الموقع ومن يحمل صلاحية إدارة الإضافة لا يرى تنبيه الطالب، وأن
    فتح مركز تحقق الطالب يحوله إلى صفحة صحة التكامل.
15. فتح `/user/profile.php` من حساب طالب مؤهل، والتأكد من ظهور حالتي البريد
    والجوال وزر مركز التحقق؛ ثم التأكد من عدم ظهوره للمدير أو عند عرض ملف طالب آخر.
16. إنشاء حساب قديم تجريبي بلا جوال، ثم اختباره في الحالتين: من جلسة الحساب
    القائمة، ومن صفحة «ربط حساب سابق» بالبريد وكلمة المرور. تأكد أن الدخول بالجوال
    يصل إلى `userid` القديم نفسه وأن الدرجات والالتحاقات لم تتغير، ثم أكمل واتساب.
17. من حساب جديد غير مكتمل التحقق ادخل بالبريد، ثم غيّر الجوال بعد إدخال كلمة
    المرور الحالية. تأكد أن الجوال الجديد يدخل إلى `userid` نفسه، وأن الجوال
    السابق مرفوض، وأن تحقق الجوال عاد إلى pending.
18. من حساب قديم بلا جوال سجل الدخول باسم المستخدم أو البريد. تأكد أن البريد
    السابق بقي مكتملًا وأن مهلة الجوال بدأت من أول دخول ولا تعاد عند إضافة رقم.
19. حاول تسجيل حساب جديد برقم موجود قديمًا في كل حالة: اسم المستخدم، alias، سجل
    تحقق، `phone1` و`phone2`. يجب أن يرفض النموذج الحساب قبل إنشائه.
20. في staging فقط أنشئ سجلًا موثقًا قديمًا وسجلًا معلقًا للرقم نفسه، ثم أرسل
    رمز الحساب المعلق. يجب أن يؤكد Webhook الحساب صاحب الرمز لا السجل الأقدم.
21. الصق رقمًا دوليًا دون `+` مع اختيار دولته، وتأكد أن مفتاح الدولة يظهر مرة
    واحدة في القيمة المحفوظة. اختبر أيضًا رقمًا محليًا ورقمًا يبدأ بـ`+`.
22. للحساب المتأثر قديمًا افتح صفحة الترحيل، واكتب User ID في إصلاح تكرار مفتاح
    الدولة. بعد الإصلاح أنشئ رمز واتساب جديدًا؛ الرمز السابق يبطل عمدًا.

## تفعيل مسار الدخول

بعد اجتياز الاختبارات فقط، يقرر المبرمج على staging هل يضبط Alternate login URL
إلى `/local/qlogin_shomokh/index.php`. الانتقال إلى الإنتاج يتم في Pull Request
مراجع، ثم نسخة احتياطية، ثم نشر ومراقبة السجلات وإمكانية الرجوع.
