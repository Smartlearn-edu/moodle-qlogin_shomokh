# مراجعة تعديلات المطور الآخر على `local_qlogin_shomokh`

التاريخ: 18 أغسطس 2026  
الملف المفحوص: `C:\Users\11\Downloads\qlogin_shomokh.zip`  
SHA-256: `46A06F03E99E4D5F7908776D1911C545F243E8F2544E44CED94B0169735643A7`

## الخلاصة

لا أوصي بتثبيت هذا الملف أو دمجه الآن. الملف المفحوص **ليس الإصدار 0.6.9 المذكور في التقرير**؛ بل هو نسخة عمل غير مكتملة بإصدار `0.6.7` ورقم بناء `2026081805`، وفيه سبعة ملفات معدلة لم تُحفظ في أي commit، كما يحتوي مجلد `.git` كاملًا.

بعض الإصلاحات موجودة وصحيحة مبدئيًا، خصوصًا تحميل `formslib.php` واستدعاء `remind($record)` في المهمة المجدولة. لكن ميزة OAuth المعلنة غير موجودة في الملف، ولا توجد أدلة قابلة لإعادة التنفيذ على اجتياز PHPUnit أو مصفوفة Moodle/PHP المعلنة.

## النتائج المؤكدة

### موانع الاعتماد

1. **عدم تطابق الإصدار والتقرير**
   - التقرير: `0.6.9` / `2026081809`.
   - الملف: `0.6.7` / `2026081805`.
   - آخر commit داخل الملف: `1b63684321148467be56a60e27cc6512d3c55477`.
   - توجد تعديلات غير محفوظة في: `classes/manager.php` و`index.php` و`link_existing.php` و`qlogin_phone.js` و`qlogin_styles.css` و`recover.php` و`version.php`.

2. **OAuth غير موجود**
   - لا يوجد في `index.php` كشف لمزودي OAuth2 أو روابط إلى `auth/oauth2`.
   - لا توجد العبارات `signin_google` أو `signin_with` أو `or_text` في ملفات اللغة.
   - توجد تنسيقات قديمة غير مستخدمة لـ`.btn-google` و`.divider` فقط؛ وهذا ليس تنفيذًا لتسجيل الدخول عبر Google.

3. **حزمة غير صالحة للنشر**
   - الملف يحتوي `.git` كاملًا بحجم يقارب 915 كيلوبايت، مع التاريخ وعنوان المستودع.
   - يجب أن تكون حزمة Moodle النهائية مجلد إضافة واحدًا نظيفًا دون `.git` أو ملفات تقارير التطوير.

4. **ادعاء الاختبارات غير قابل للتحقق من الملف**
   - لا توجد إعدادات CI أو `composer.json` أو مخرجات اختبار محفوظة.
   - ملف `PRODUCTION_READINESS_TEST_PLAN.md` يحتوي 67 بندًا غير محدد و0 بند مكتمل.
   - نجح الفحص النحوي فقط، لكنه لا يثبت سلامة التشغيل أو التوافق مع كل نسخ Moodle/PHP.

### تعارضات أخرى مع التقرير

1. التقرير يقول إن `index.php` أصبح PHP نقيًا باستخدام `html_writer`، لكن الملف ما زال يغلق PHP عند السطر 171 ويكتب HTML ممزوجًا حتى نهاية الصفحة.
2. التقرير يذكر قواعد عزل Bootstrap مثل `col-md-3` و`col-md-9` و`flex: 0 0 100%`، وهذه القواعد غير موجودة في CSS المرسل.
3. التقرير ينسب استعادة منطق المهمة المجدولة إلى تعديلات اليوم، لكن مقارنة تاريخ Git تُظهر أن المنطق الجوهري كان موجودًا في النسخة الأساسية؛ التغيير الظاهر في الملف الحالي تنسيقي غالبًا.
4. تقرير داخلي داخل الحزمة يقول 68 ملف PHP، بينما التقرير المرسل يقول 69؛ الفحص الفعلي وجد 69 ملفًا.

### ملاحظات فنية على التعديلات الجديدة غير المحفوظة

1. إضافة أسماء الدول المترجمة فكرة صحيحة، واستدعاء `get_list_of_countries(true, $lang)` متاح في Moodle 4.1.
2. محددات CSS التالية غير معزولة عن بقية الموقع: `body.dir-rtl .iti__country-list` وما شابهها. قد تؤثر في أي حقل هاتف آخر يستخدم `intl-tel-input`. يجب أن تصبح مثل `body.dir-rtl #qlogin-wrapper ...`.
3. تعديل واجهة الدول لم يُدعّم باختبار متصفح محفوظ، والصور السابقة أظهرت انتقال القائمة إلى جانب الصفحة. يلزم اختباره على القالب الفعلي، RTL وLTR، والجوال.
4. `localized_countries_json()` يعلن أنه يعيد `string` بينما `json_encode()` قد يعيد `false`. الاحتمال ضعيف مع أسماء دول Moodle، لكن الأفضل معالجة الخطأ صراحةً.
5. توجد CSS لـGoogle دون عنصر واجهة يستخدمها؛ تُحذف إن لم تعتمد OAuth، أو تكتمل الميزة في تغيير مستقل.

## ما نجح في الفحص المستقل

- `php -l`: نجح على 69/69 ملف PHP.
- `node --check`: نجح على 2/2 ملف JavaScript.
- `db/install.xml`: XML صالح نحويًا.
- ملفات اللغة: 346 مفتاحًا عربيًا و346 إنجليزيًا دون مفاتيح ناقصة.
- لم أعثر على مفتاح Resend أو Meta حقيقي داخل المصدر.
- نماذج Moodle العشرة تحمل `formslib.php`، وحقول كود الدولة تستخدم `PARAM_INT`.
- المهمة المجدولة تمرر السجل إلى `verification::remind($record)`، لذلك خطأ نقص الوسيط المذكور لا يظهر في هذا المصدر.

## ما لم يُثبت بعد

- PHPUnit داخل Moodle حقيقي.
- Moodle Plugin CI وCodeChecker على نفس commit النهائي.
- تثبيت نظيف وترقية من النسخة المركبة حاليًا.
- توافق Moodle 4.1 و4.2 و4.3 و4.4 و4.5 كلٌ مع نسخة PHP المدعومة له.
- تجربة Google/OAuth؛ لأنها غير موجودة أصلًا في الملف.
- Cron الفعلي، Resend، Webhooks، ورسائل WhatsApp في staging.
- واجهة Educard على Chrome وFirefox وSafari والجوال، بالعربية والإنجليزية.

## أسئلة جاهزة لإرسالها إلى المطور

> شكرًا على التقرير. الملف الذي وصلنا لا يطابقه: `version.php` فيه 0.6.7 / 2026081805، وليس 0.6.9 / 2026081809، كما أن Git يظهر سبعة ملفات غير محفوظة، والحزمة تحتوي مجلد `.git`. فضلاً أرسل ZIP نظيفًا للإصدار 0.6.9 مع commit hash أو tag مطابق، دون `.git`.
>
> 1. أين تنفيذ OAuth2 المذكور؟ الملف الحالي لا يحتوي اكتشاف issuers أو روابط `auth/oauth2` أو نصوص اللغة، بل يحتوي CSS غير مستخدم فقط.
> 2. هل تقرير 0.6.9 يخص ملفًا آخر؟ لأنه يقول إن `index.php` أصبح PHP نقيًا وإن قواعد عزل Bootstrap أضيفت، وكلاهما غير موجود في الملف المرسل.
> 3. فضلاً أرسل رابط تشغيل CI أو المخرجات الخام والأوامر الدقيقة على commit النهائي، مع نتائج PHPUnit؛ فخطة الاختبار المرفقة كلها غير محددة.
> 4. ما نسخ Moodle وPHP التي شُغلت فعليًا، وليس فحص syntax فقط؟ نحتاج مصفوفة واضحة ونتيجة تثبيت نظيف وترقية.
> 5. ما الإصدار الذي ظهر فيه خطأ `remind()`؟ مقارنة الملف الحالي بالتاريخ المرفق لا تظهر تغييرًا وظيفيًا واضحًا في Cron. نحتاج diff أو اختبارًا يثبت الإصلاح.
> 6. هل تعديلات ترجمة الدول وRTL السبعة مقصودة ضمن 0.6.9؟ إن كانت كذلك، رجاءً حفظها في commit مستقل وعزل كل CSS تحت `#qlogin-wrapper` وإرفاق صور اختبار للجوال وRTL/LTR.
> 7. هل OAuth مطلوب فعلًا ضمن هذه الإضافة أم سيبقى Moodle OAuth القياسي منفصلًا؟ نفضّل قرارًا معماريًا واضحًا كي لا نضيف مسار دخول ثالثًا قبل اختباره.
> 8. هل توجد أي تغييرات قاعدة بيانات بعد 2026081801؟ إن وجدت، يجب أن تظهر بوضوح في `install.xml` و`upgrade.php` مع اختبار ترقية.

## خطة الدمج الآمنة

1. تجميد النسخة الحالية السليمة وعدم استبدالها بهذا ZIP.
2. استلام إصدار 0.6.9 نظيف ومطابق لـcommit أو Pull Request معروف.
3. مقارنة وظيفية من القاعدة المشتركة، لا نسخ 69 ملفًا دفعة واحدة؛ وفصل تغييرات التنسيق عن تغييرات السلوك.
4. دمج كل محور على حدة:
   - إصلاح النماذج.
   - Cron مع اختبار آلي.
   - ترجمة الدول وRTL مع عزل CSS واختبار بصري.
   - OAuth في Pull Request مستقل واختياري بعد الاتفاق على الحاجة إليه.
5. تشغيل `php -l` وJavaScript check وCodeChecker وPHPUnit، ثم تثبيت نظيف وترقية على Moodle مطابق للإنتاج.
6. اختبار التدفقات الأساسية على staging: مستخدم جديد، حساب قديم، رقم مكرر، ربط الجوال، البريد، WhatsApp، الاستعادة وCron.
7. اختبار الواجهة على Educard بالعربية والإنجليزية وعلى الهاتف والحاسوب.
8. إنشاء ZIP نظيف دون `.git` أو أسرار، وتسجيل SHA-256 والإصدار والـcommit قبل تسليمه للتثبيت.

## القرار المقترح لكل محور

| المحور | القرار الحالي |
|---|---|
| `formslib.php` و`PARAM_INT` | صالح مبدئيًا بعد PHPUnit |
| Cron | الكود الحالي أفضل من الخطأ المبلغ عنه، لكنه يحتاج اختبارًا فعليًا |
| ترجمة أسماء الدول | مفيدة، تُقبل بعد عزل CSS واختبار المتصفحات |
| OAuth/Google | غير موجود؛ لا يمكن مراجعته أو اعتماده |
| إعادة تنسيق عشرات الملفات | لا تدمج جماعيًا؛ تسبب تعارضات دون فائدة وظيفية واضحة |
| ZIP الحالي | مرفوض للنشر أو الدمج |
