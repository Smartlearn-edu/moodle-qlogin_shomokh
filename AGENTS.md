# AGENTS.md

تعليمات ملزمة لأي وكيل أو مطور يعمل داخل `local/qlogin_shomokh`.

## قبل أي تعديل

1. اقرأ هذا الملف كاملًا، ثم اقرأ `CONTEXT.md` و`README.md` و`PORTABLE_ARCHITECTURE_AR.md` والملفات المرتبطة مباشرة بالمهمة.
2. افحص `version.php` و`db/install.xml` و`db/upgrade.php` قبل أي تغيير في البيانات.
3. لا تفترض أن نسخة الإنتاج مطابقة لبيئة الاختبار. اطلب أو وثّق إصدار Moodle وPHP وقاعدة البيانات والإضافات المتداخلة.
4. لا تعدّل Moodle Core ولا `local_qlogin` ولا `local_phoneverify` من هذه الإضافة.

## أدلة الاختبار التي لا تُعاد بلا سبب

- اعتبر النتائج المؤرخة في `CONTEXT.md` دليلًا صالحًا للكود والإعدادات التي اختُبرت على
  `test.smartlearn.education`. لا تُعد التسجيلات اليدوية نفسها لمجرد فهم المشروع.
- أعد الاختبار المتأثر فقط عندما يتغير الكود، أو النطاق، أو رقم واتساب، أو Phone Number ID، أو
  WABA/App، أو الأسرار، أو مزود البريد، أو إصدار المنصة.
- عند تركيب نسخة جديدة استخدم اختبار المدير في `health.php`: لا تنشئ طالبة تجريبية ولا تغيّر
  حالة حساب قائم لاختبار Webhook.
- العلامتان الرماديتان في واتساب تثبتان التسليم إلى واتساب، وليستا نتيجة Webhook. النتيجة المرجعية
  هي حدث `integrationtest/passed` أو `verification/verified` داخل سجل الإضافة.

## قواعد التطوير

- استخدم Moodle APIs فقط: Moodle Forms، `$DB` بمعاملات مربوطة، `moodle_url`، الصلاحيات و`sesskey`.
- لا تستخدم `$_GET` أو `$_POST`. الاستثناء الوحيد للـwebhooks هو جسم HTTP الخام والترويسات اللازمة للتحقق من التوقيع.
- لا تضع نصوص واجهة ثابتة؛ أضف مفاتيح متطابقة في `lang/en` و`lang/ar`.
- لا تضع مفاتيح Resend أو Meta أو كلمات مرور في الكود أو ZIP أو Git. القيم الخارجية تتقدم على إعدادات قاعدة البيانات.
- لا تضف Composer dependency أو CDN دون ضرورة وموافقة واضحة. استخدم Moodle `\curl` لواجهة Resend.
- رموز التحقق والاستعادة أحادية الاستخدام، قصيرة العمر، ولا تحفظ خامًا في جداول الإضافة أو بيانات المهام المجدولة.
- لا تسجل البريد أو الجوال كاملين في السجلات التقنية. استخدم قيمة مخفية أو hash عند الحاجة.
- لا تغيّر أسماء مستخدمي الحسابات القديمة جماعيًا. استخدم جدول alias يحافظ على `userid` والدرجات والالتحاقات.
- حلّ الدخول يقبل الجوال الحالي أو البريد الفريد أو اسم المستخدم الدقيق. لا تختر
  حسابًا آليًا عند تكرار البريد، ولا تسمح للجوال القديم بالدخول بعد وجود alias نشط مختلف.
- قبل إنشاء حساب أو تغيير جوال استخدم `migration::phone_in_use()` داخل قفل الرقم؛
  لا تكتفِ بـ`user.username` لأن الحساب القديم قد يحتفظ بالرقم في alias أو سجل
  تحقق أو `phone1` أو `phone2` فقط.
- عند معالجة واتساب لا تختر أول سجل بالرقم. طابق hash الرمز بين السجلات المعلقة
  أولًا؛ فقد تحتوي البيانات القديمة سجلًا موثقًا وحسابًا معلقًا للرقم نفسه.
- حقل الجوال عالمي. عند لصق رقم يبدأ بمفتاح الدولة دون `+` لا تضف مفتاح الحقل
  مرة أخرى. استخدم بلد picker الفعلي، واختبر حالة `966966…` وحالات مفاتيح الدول
  ذات رقم أو رقمين قبل التسليم.
- إصلاح تكرار مفتاح الدولة إداري وحتمي عبر User ID؛ لا تحذف سجلات تحقق تاريخية
  ولا تغير `userid`، ولا تتجاوز ملكية username أو alias أو حقول جوال حساب آخر.
- تغيير الجوال يتطلب جلسة المستخدم وإعادة التحقق من كلمة المرور، ويحافظ على
  `userid` واسم المستخدم الداخلي. يصبح الرقم الجديد pending، ولا يعاد موعد
  المهلة إذا كان الرقم السابق غير موثّق.
- سجّل قنوات الحسابات الجديدة عند حدث `user_created`. اعتماد بريد قديم مسموح فقط
  للحساب القديم غير المتتبع كليًا أو عبر إجراء ترحيل إداري صريح؛ لا تستنتج أن
  بريد حساب جديد موثّق من مجرد وجوده في جدول المستخدم.
- لا تطابق حسابًا قديمًا بالاسم. الربط الذاتي يتطلب جلسة الحساب أو كلمة مروره، والبريد المكرر
  لا يُحل تلقائيًا، والجوال لا يصبح موثوقًا حتى ينجح تحقق واتساب.
- أي ترحيل يبدأ بتقرير dry-run، ثم إجراء صريح محمي بصلاحية و`sesskey`، ويكون idempotent وقابلًا للاستكمال.
- لا تجعل فشل البريد يلغي حسابًا أُنشئ بنجاح. أبلغ المستخدم، وسجّل المحاولة، وأعد المحاولة بأمان.
- لا ترسل الرسالة نفسها عبر Resend وMoodle في الوقت نفسه. يوجد مزود أساسي واحد، والفشل المؤقت يستخدم إعادة محاولة بمعرف idempotency.

## بنية الكود

- `classes/mail/`: مزودات البريد، النتائج، والسجل التقني.
- `classes/email_recovery.php`: استعادة البريد المستقلة.
- `classes/migration.php`: الفحص والربط الآمن للحسابات القديمة.
- `classes/account_link.php` و`link_existing.php`: حل وسائل الدخول، وإثبات الحساب، وربط الجوال أو تغييره.
- `classes/task/`: إعادة المحاولة والمعالجة الدورية فقط؛ لا تخزن أسرارًا خامًا في `customdata`.
- صفحات الإدارة تتطلب `local/qlogin_shomokh:manage` في سياق النظام.
- لا تعرض تنبيهات الطالب أو مركز تحقق الطالب لمن يحمل صلاحية الإدارة. اختبارات الإدارة مستقلة
  تمامًا عن جداول تحقق المستخدمين.
- رابط التحقق داخل ملف المستخدم يُضاف عبر callback Moodle الرسمي
  `local_qlogin_shomokh_myprofile_navigation`. لا تعدّل `/user/profile.php` أو القالب لإضافته،
  ولا تعرضه إلا لصاحب الحساب المؤهل نفسه.

## عند نقل الفكرة إلى منصة أخرى

- لا تنقل ملفات Moodle حرفيًا. انقل العقود والحالات ومبادئ الأمان الموضحة في
  `PORTABLE_ARCHITECTURE_AR.md`، ثم نفّذ adapters خاصة بإطار العمل.
- افصل دائمًا: `Identity`، و`Verification service`، و`Email provider`، و`WhatsApp webhook`،
  و`Admin diagnostics`، و`Legacy alias migration`.
- في Next.js اجعل الأسرار وعمليات المطابقة في الخادم فقط، واقرأ جسم Webhook الخام قبل JSON،
  واستخدم unique constraint لمعرّف الرسالة.
- في Frappe خزّن الأسرار في `site_config` أو مدير أسرار، واستخدم DocTypes مستقلة للحالة والأحداث،
  واسمح للضيف في Webhook فقط مع تحقق التوقيع قبل أي معالجة.
- حافظ على المعرّف الداخلي القديم للمستخدم عند الترحيل في أي منصة. رقم الجوال alias للدخول، وليس
  سببًا لإنشاء سجل مستخدم جديد أو نقل الدرجات.

## قاعدة البيانات والترقية

- حد أسماء جداول XMLDB المتوافق مع Moodle 4.1/4.2 هو 28 حرفًا.
- حدّث `db/install.xml` للتركيب الجديد و`db/upgrade.php` للترقية القائمة، ثم ارفع رقم `version.php`.
- لا تجمع foreign-unique key مع index فريد على الحقول نفسها.
- أعمدة `CHAR NOT NULL` لا تستخدم قيمة افتراضية فارغة في XMLDB.
- حافظ على بيانات الإصدارات السابقة، ولا تحذف جدولًا قديمًا إلا بعد نقل idempotent ومثبت.

## التحقق قبل التسليم

1. `php -l` لكل ملفات PHP المعدلة.
2. التحقق من XML ومساواة مفاتيح اللغتين العربية والإنجليزية.
3. تشغيل PHPUnit المتاح، وإضافة اختبارات للوحدات الجديدة قدر الإمكان.
4. فحص ZIP: مجلد جذر واحد `qlogin_shomokh/`، ولا أسرار أو `.git` أو ملفات بيئة.
5. اختبار staging: التسجيل، الدخول، التحقق، استعادة البريد وواتساب، زر العين، إعادة المحاولة، health check، وdry-run للحسابات القديمة.
   بعد إثبات تدفق الطالب مرة واحدة، استخدم اختبار المدير المدمج لإعادة فحص واتساب عند تغيير المواقع
   أو الأرقام.
6. لا تفعّل Alternate login URL ولا تنشر على الإنتاج قبل نجاح الاختبارات والنسخة الاحتياطية.
